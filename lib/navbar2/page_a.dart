
import 'package:flutter/material.dart';
class Access_Alarm extends StatelessWidget {
  const Access_Alarm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Hello')),
      ),
    );
  }
}
