import 'package:flutter/material.dart';
class Accessible extends StatelessWidget {
  const Accessible({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
